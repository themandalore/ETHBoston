/// <reference types="node" />
import type Api from "@irys/arweave/build/cjs/common/lib/api";
import type { SignatureOptions } from "@irys/arweave/build/cjs/common/lib/crypto/crypto-interface";
import type CryptoInterface from "@irys/arweave/build/cjs/common/lib/crypto/crypto-interface";
import Transaction from "@irys/arweave/build/cjs/common/lib/transaction";
import type { JWKInterface } from "@irys/arweave/build/cjs/common/lib/wallet";
import type { SerializedUploader } from "@irys/arweave/build/cjs/common/lib/transaction-uploader";
import { TransactionUploader } from "@irys/arweave/build/cjs/common/lib/transaction-uploader";
import type Chunks from "@irys/arweave/build/cjs/common/chunks";
import type FallbackApi from "@irys/arweave/build/cjs/common/lib/fallbackApi";
import type Merkle from "@irys/arweave/build/cjs/common/lib/merkle";
import type { DeepHash } from "@irys/arweave/build/cjs/common/lib/deepHash";
export type TransactionConfirmedData = {
    block_indep_hash: string;
    block_height: number;
    number_of_confirmations: number;
};
export type TransactionStatusResponse = {
    status: number;
    confirmed: TransactionConfirmedData | null;
};
export default class Transactions {
    private api;
    private crypto;
    private chunks;
    protected merkle: Merkle;
    protected deepHash: DeepHash;
    constructor({ deps }: {
        deps: {
            crypto: CryptoInterface;
            api: Api | FallbackApi;
            chunks: Chunks;
            merkle: Merkle;
            deepHash: DeepHash;
        };
    });
    getTransactionAnchor(): Promise<string>;
    getPrice(byteSize: number, targetAddress?: string): Promise<string>;
    get(id: string): Promise<Transaction>;
    fromRaw(attributes: object): Transaction;
    getStatus(id: string): Promise<TransactionStatusResponse>;
    getData(id: string): Promise<Uint8Array>;
    getDataStream(id: string): Promise<AsyncIterable<Uint8Array>>;
    sign(transaction: Transaction, jwk?: JWKInterface | "use_wallet", // "use_wallet" for backwards compatibility only
    options?: SignatureOptions): Promise<void>;
    verify(transaction: Transaction): Promise<boolean>;
    post(transaction: Transaction | Buffer | string | object): Promise<{
        status: number;
        statusText: string;
        data: any;
    }>;
    /**
     * Gets an uploader than can be used to upload a transaction chunk by chunk, giving progress
     * and the ability to resume.
     *
     * Usage example:
     *
     * ```
     * const uploader = arweave.transactions.getUploader(transaction);
     * while (!uploader.isComplete) {
     *   await uploader.uploadChunk();
     *   console.log(`${uploader.pctComplete}%`);
     * }
     * ```
     *
     * @param upload a Transaction object, a previously save progress object, or a transaction id.
     * @param data the data of the transaction. Required when resuming an upload.
     */
    getUploader(upload: Transaction | SerializedUploader | string, data?: Uint8Array | ArrayBuffer): Promise<TransactionUploader>;
    /**
     * Async generator version of uploader
     *
     * Usage example:
     *
     * ```
     * for await (const uploader of arweave.transactions.upload(tx)) {
     *  console.log(`${uploader.pctComplete}%`);
     * }
     * ```
     *
     * @param upload a Transaction object, a previously save uploader, or a transaction id.
     * @param data the data of the transaction. Required when resuming an upload.
     */
    upload(upload: Transaction | SerializedUploader | string, data: Uint8Array): AsyncGenerator<TransactionUploader, TransactionUploader>;
}

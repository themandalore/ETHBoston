import type Irys from "@irys/sdk/build/cjs/common/irys";
import type { TxGqlNode } from "@irys/sdk/build/cjs/common/types";
export declare class Transaction {
    protected irys: Irys;
    constructor(irys: Irys);
    getById(id: string): Promise<TxGqlNode>;
    getByOwner(owner: string): Promise<TxGqlNode>;
    getByTag(name: string, value: string): Promise<TxGqlNode>;
    query(parameters: {
        order?: "desc" | "asc";
        ids?: string[];
        limit?: number;
        after?: string;
        currency?: string;
        owners?: string[];
        hasTags?: boolean;
        tags?: {
            name: string;
            values: string[];
        }[];
    }): Promise<TxGqlNode[]>;
}
